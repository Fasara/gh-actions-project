name: Setup Node.js project
description: Checks out the repo, installs Node.js, and installs dependencies

runs:
  using: composite
  steps:
    - name: Get code
      uses: actions/checkout@v6
    - name: Install Node.js
      uses: actions/setup-node@v6
    - name: Install dependencies
      run: npm ci
      shell: bash
